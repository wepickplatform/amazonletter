'use client'

export default function FeaturedSlider({ posts }) {
  return (
    <div className="bg-gray-200 h-[500px] rounded-xl flex items-center justify-center">
      <p className="text-gray-600">슬라이더가 여기에 표시됩니다</p>
    </div>
  );
}
